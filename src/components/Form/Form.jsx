/* const Form = ({ onSubmit, onChange, newNota }) => {
  return (
    <div>
      <form onSubmit={onSubmit} value={newNota}>
        <input type="text" defaultValue={newNota} onChange={onChange} ref={(input) => input && input.focus()} />
        <button type="submit">Agregar</button>
      </form>
    </div>
  );
};

export default Form;
 */